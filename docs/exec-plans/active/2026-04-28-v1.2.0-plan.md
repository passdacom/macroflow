# MacroFlow v1.2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1.2.0 기능 5종 구현: 텍스트 입력 딜레이 설정, 상대 시간 표시, 클릭 이벤트 추가 다이얼로그, 위치 변경 시 색 자동 업데이트(버그), 즐겨찾기 검색 필터

**Architecture:** 4개 태스크. Task 1~3은 `editor.py`, Task 4는 `favorites.py`. editor.py는 mypy `ignore_errors = true` 대상. favorites.py는 mypy strict 검사 대상 — 타입 어노테이션 필수.

**Tech Stack:** Python 3.11+, PyQt6, dataclasses, secrets

---

## 파일 맵

| 파일 | 변경 내용 |
|---|---|
| `src/macroflow/ui/editor.py` | NEW-01(텍스트 딜레이), NEW-02(상대시간), NEW-03(클릭추가), NEW-04(위치→색 업데이트) |
| `src/macroflow/ui/favorites.py` | NEW-05(검색 필터) |
| `tests/test_editor_relative_time.py` | NEW-02 단위 테스트 |
| `tests/test_favorites_search.py` | NEW-05 단위 테스트 |

---

## Task 1: NEW-01 텍스트 입력 딜레이 + NEW-04 위치 변경 시 색 업데이트

**Files:**
- Modify: `src/macroflow/ui/editor.py:20-41` (imports — QLineEdit 추가)
- Modify: `src/macroflow/ui/editor.py:1426-1476` (`_insert_text_input`)
- Modify: `src/macroflow/ui/editor.py:1071-1134` (`_edit_position`)

### NEW-01: 텍스트 입력 다이얼로그에 딜레이 스핀박스 추가

현황: `_insert_text_input`이 `QInputDialog.getText`로 텍스트만 받고 `_BUDGET_NS = 1_000_000_000` (1초) 하드코딩.
요구사항: 커스텀 QDialog로 교체. 텍스트 입력란 + 딜레이 스핀박스(0-30000ms, 기본 1000). 입력한 딜레이가 `delay_override_ms`와 타임스탬프 예산 양쪽에 반영.

- [ ] **Step 1: QLineEdit import 추가**

`editor.py` 상단 imports의 `QWidget` 앞에 `QLineEdit,`를 추가한다.

변경 전:
```python
from PyQt6.QtWidgets import (
    QAbstractItemView,
    QDialog,
    QDialogButtonBox,
    QDoubleSpinBox,
    QFormLayout,
    QGroupBox,
    QHBoxLayout,
    QHeaderView,
    QInputDialog,
    QLabel,
    QMenu,
    QMessageBox,
    QPushButton,
    QRadioButton,
    QSpinBox,
    QTableWidget,
    QTableWidgetItem,
    QToolBar,
    QVBoxLayout,
    QWidget,
)
```

변경 후:
```python
from PyQt6.QtWidgets import (
    QAbstractItemView,
    QCheckBox,
    QDialog,
    QDialogButtonBox,
    QDoubleSpinBox,
    QFormLayout,
    QGroupBox,
    QHBoxLayout,
    QHeaderView,
    QInputDialog,
    QLabel,
    QLineEdit,
    QMenu,
    QMessageBox,
    QPushButton,
    QRadioButton,
    QSpinBox,
    QTableWidget,
    QTableWidgetItem,
    QToolBar,
    QVBoxLayout,
    QWidget,
)
```

(QCheckBox는 Task 2에서 사용. 한 번에 추가.)

- [ ] **Step 2: `_insert_text_input` 메서드 교체**

`_insert_text_input` 전체를 아래 코드로 교체한다:

```python
def _insert_text_input(self, row_idx: int) -> None:
    """선택 행 다음에 TextInputEvent를 삽입한다.

    커스텀 다이얼로그로 텍스트 + 딜레이(ms)를 받아 이벤트 삽입.
    딜레이 값이 delay_override_ms와 타임스탬프 예산에 모두 반영.
    """
    if self._macro is None:
        return

    dialog = QDialog(self)
    dialog.setWindowTitle("텍스트 입력 추가")
    dialog.setFixedWidth(380)

    form = QFormLayout()
    text_edit = QLineEdit()
    text_edit.setPlaceholderText("한글·영문·숫자·특수문자·이모지 모두 지원")
    form.addRow("텍스트:", text_edit)

    delay_spin = QSpinBox()
    delay_spin.setRange(0, 30000)
    delay_spin.setValue(1000)
    delay_spin.setSuffix(" ms")
    delay_spin.setToolTip("이 이벤트 실행 전 대기 시간 (delay_override_ms)")
    form.addRow("딜레이:", delay_spin)

    buttons = QDialogButtonBox(
        QDialogButtonBox.StandardButton.Ok | QDialogButtonBox.StandardButton.Cancel
    )
    buttons.accepted.connect(dialog.accept)
    buttons.rejected.connect(dialog.reject)

    v = QVBoxLayout(dialog)
    v.addLayout(form)
    v.addWidget(buttons)

    if dialog.exec() != QDialog.DialogCode.Accepted:
        return

    text = text_edit.text()
    if not text:
        return

    delay_ms = delay_spin.value()
    _BUDGET_NS = max(delay_ms * 1_000_000, 1_000_000)  # 최소 1ms

    rows = self._selected_row_indices()
    if rows:
        last_row = self._rows[rows[-1]]
        insert_after_event_idx = max(last_row.event_indices)
    else:
        insert_after_event_idx = len(self._macro.events) - 1

    evs = self._macro.events
    if 0 <= insert_after_event_idx < len(evs):
        prev_ts_ns = evs[insert_after_event_idx].timestamp_ns
    elif evs:
        prev_ts_ns = evs[-1].timestamp_ns
    else:
        prev_ts_ns = 0

    new_event = TextInputEvent(
        id=secrets.token_hex(4),
        type="text_input",
        timestamp_ns=prev_ts_ns + _BUDGET_NS,
        delay_override_ms=delay_ms if delay_ms > 0 else None,
        text=text,
    )
    self._push_undo()
    events = list(self._macro.events)
    events.insert(insert_after_event_idx + 1, new_event)

    for i in range(insert_after_event_idx + 2, len(events)):
        ev = events[i]
        events[i] = dataclasses.replace(ev, timestamp_ns=ev.timestamp_ns + _BUDGET_NS)

    self._apply_events(events)
```

- [ ] **Step 3: ruff check**

```bash
cd /root/.openclaw/workspace/macroflow && ruff check .
```

Expected: no errors (또는 기존 오류만).

### NEW-04: 위치 변경 시 recorded_color 자동 업데이트

현황: `_edit_position`의 F6 콜백 `_on_f6_captured(x_r, y_r, _color)`가 `_color`를 무시(underscore prefix).
요구사항: F6 캡처로 위치를 지정한 경우에만, 수락 후 해당 mouse_down 이벤트의 `recorded_color`를 캡처된 색으로 업데이트.

- [ ] **Step 4: `_edit_position` 수정 — 색 캡처 클로저 추가**

`_edit_position` 내 `_on_f6_captured` 함수와 그 전후를 다음과 같이 수정한다.

기존 코드 (line 1071 근처):
```python
        def _on_f6_captured(x_r: float, y_r: float, _color: str) -> None:
            """F6 캡처 콜백 — 다이얼로그 복원 및 SpinBox 갱신."""
            x_spin.setValue(x_r * 100)
            y_spin.setValue(y_r * 100)
            capture_label.setText(f"✅ 캡처됨: ({x_r * 100:.1f}%, {y_r * 100:.1f}%)")
            dialog.showNormal()
            dialog.raise_()
            btn_capture.setEnabled(True)
```

수정 후:
```python
        captured_color: list[str] = []  # [color_hex] if F6 was used, else []

        def _on_f6_captured(x_r: float, y_r: float, color: str) -> None:
            """F6 캡처 콜백 — 다이얼로그 복원 및 SpinBox 갱신."""
            captured_color.clear()
            captured_color.append(color)
            x_spin.setValue(x_r * 100)
            y_spin.setValue(y_r * 100)
            capture_label.setText(f"✅ 캡처됨: ({x_r * 100:.1f}%, {y_r * 100:.1f}%)")
            dialog.showNormal()
            dialog.raise_()
            btn_capture.setEnabled(True)
```

- [ ] **Step 5: `_edit_position` 수락 후 색 업데이트 로직 추가**

`dialog.exec()` 이후, `self._macro = new_macro` / `_refresh` / `emit` 직전에 색 업데이트 블록을 삽입한다.

기존 코드 (line 1120 근처, 그룹 내 나머지 이벤트 업데이트 블록 끝):
```python
        self._macro = new_macro
        self._refresh()
        self.macro_changed.emit(self._macro)
```

수정 후 (위치 업데이트 루프 끝 ~ self._macro 할당 사이에 삽입):
```python
        # F6 캡처로 위치를 지정한 경우 mouse_down의 recorded_color를 업데이트
        if captured_color and isinstance(primary, MouseButtonEvent):
            color_hex = captured_color[0]
            updated_events = list(new_macro.events)
            for idx2, ev2 in enumerate(updated_events):
                if ev2.id == primary.id and isinstance(ev2, MouseButtonEvent):
                    updated_events[idx2] = dataclasses.replace(ev2, recorded_color=color_hex)
                    break
            new_macro = MacroData(
                meta=new_macro.meta,
                settings=new_macro.settings,
                raw_events=new_macro.raw_events,
                events=updated_events,
                is_edited=True,
            )

        self._macro = new_macro
        self._refresh()
        self.macro_changed.emit(self._macro)
```

- [ ] **Step 6: ruff check + 커밋**

```bash
cd /root/.openclaw/workspace/macroflow && ruff check .
```

Expected: no errors.

```bash
git add src/macroflow/ui/editor.py
git commit -m "feat(editor): text input delay spinbox + position F6 color capture (NEW-01, NEW-04)"
```

---

## Task 2: NEW-02 시간 열 상대/절대 전환 체크박스

**Files:**
- Modify: `src/macroflow/ui/editor.py:106-118` (`_DisplayRow` 필드 추가)
- Modify: `src/macroflow/ui/editor.py:125-400` (`_build_rows` 후처리)
- Modify: `src/macroflow/ui/editor.py:496-576` (toolbar 체크박스 추가)
- Modify: `src/macroflow/ui/editor.py:762-775` (테이블 렌더링)
- Create: `tests/test_editor_relative_time.py`

### 구현

현황: 시간(ms) 열은 항상 절대 시간(녹화 시작 기준).
요구사항: 툴바에 "⏱ 상대 시간" 체크박스 추가. 체크 시 이전 이벤트 대비 delta(ms) 표시. `delay_override_ms`가 있으면 그 값 우선.

- [ ] **Step 1: 테스트 작성 (실패 확인용)**

`tests/test_editor_relative_time.py` 파일을 생성한다:

```python
"""_build_rows 상대 시간 계산 단위 테스트."""

from __future__ import annotations

from macroflow.types import (
    MacroMeta,
    MacroSettings,
    MacroData,
    MouseButtonEvent,
    MouseMoveEvent,
)
from macroflow.ui.editor import _build_rows


def _make_mouse_down(id_: str, ts_ns: int, x: float = 0.5, y: float = 0.5,
                     delay_ms: int | None = None) -> MouseButtonEvent:
    return MouseButtonEvent(
        id=id_, type="mouse_down", timestamp_ns=ts_ns,
        x_ratio=x, y_ratio=y, button="left",
        delay_override_ms=delay_ms,
    )


def _make_mouse_up(id_: str, ts_ns: int, x: float = 0.5, y: float = 0.5) -> MouseButtonEvent:
    return MouseButtonEvent(
        id=id_, type="mouse_up", timestamp_ns=ts_ns,
        x_ratio=x, y_ratio=y, button="left",
    )


class TestRelativeTime:
    def test_first_row_equals_absolute(self) -> None:
        """첫 번째 행의 time_ms_rel은 절대값과 같아야 한다."""
        down = _make_mouse_down("a", 500_000_000)  # 500ms
        up = _make_mouse_up("b", 600_000_000)
        rows = _build_rows([down, up], show_moves=False)
        assert len(rows) == 1
        assert rows[0].time_ms_rel == pytest.approx(500.0, abs=1.0)

    def test_second_row_is_delta(self) -> None:
        """두 번째 행의 time_ms_rel은 이전 primary ts와의 차이."""
        events = [
            _make_mouse_down("a", 100_000_000),
            _make_mouse_up("b", 200_000_000),
            _make_mouse_down("c", 350_000_000),
            _make_mouse_up("d", 450_000_000),
        ]
        rows = _build_rows(events, show_moves=False)
        assert len(rows) == 2
        # 350ms - 100ms = 250ms
        assert rows[1].time_ms_rel == pytest.approx(250.0, abs=1.0)

    def test_delay_override_takes_precedence(self) -> None:
        """delay_override_ms가 있으면 그 값이 time_ms_rel로 사용되어야 한다."""
        events = [
            _make_mouse_down("a", 100_000_000),
            _make_mouse_up("b", 200_000_000),
            _make_mouse_down("c", 999_000_000, delay_ms=500),  # override=500
            _make_mouse_up("d", 1100_000_000),
        ]
        rows = _build_rows(events, show_moves=False)
        assert rows[1].time_ms_rel == pytest.approx(500.0, abs=1.0)


import pytest
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

```bash
cd /root/.openclaw/workspace/macroflow
source .venv/bin/activate && python3 -m pytest tests/test_editor_relative_time.py -v
```

Expected: `AttributeError: '_DisplayRow' object has no attribute 'time_ms_rel'` (또는 ImportError)

- [ ] **Step 3: `_DisplayRow`에 `time_ms_rel` 필드 추가**

`_DisplayRow` 데이터클래스에 마지막 필드로 추가:

기존:
```python
    color_check_on_mismatch: str = "skip"     # "skip" | "stop" | "wait"
```

변경 후:
```python
    color_check_on_mismatch: str = "skip"     # "skip" | "stop" | "wait"
    time_ms_rel: float = 0.0                  # 이전 이벤트 대비 delta(ms). 상대 시간 표시용.
```

- [ ] **Step 4: `_build_rows` 끝에 상대 시간 후처리 추가**

`_build_rows` 함수의 `return rows` 직전에 다음 블록을 추가한다:

```python
    # 상대 시간 계산: 각 행의 primary 이벤트 기준
    for i, row in enumerate(rows):
        ev = events[row.primary_idx]
        if ev.delay_override_ms is not None:
            row.time_ms_rel = float(ev.delay_override_ms)
        elif i == 0:
            row.time_ms_rel = row.time_ms  # 첫 행은 절대값 그대로
        else:
            prev_ev = events[rows[i - 1].primary_idx]
            row.time_ms_rel = (ev.timestamp_ns - prev_ev.timestamp_ns) / 1_000_000

    return rows
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
source .venv/bin/activate && python3 -m pytest tests/test_editor_relative_time.py -v
```

Expected: 3 passed.

- [ ] **Step 6: `_setup_ui`에 체크박스 추가**

`editor.py` `_setup_ui` 메서드에서, `self._act_reset = QAction("원본 복원", self)` 블록이 끝난 뒤 (현재 `layout.addWidget(toolbar)` 직전)에 체크박스를 추가한다:

기존 (`layout.addWidget(toolbar)` 바로 위):
```python
        toolbar.addSeparator()

        self._act_reset = QAction("원본 복원", self)
        self._act_reset.setToolTip("raw_events 기준으로 events를 초기화합니다")
        self._act_reset.triggered.connect(self._reset_to_raw)
        self._act_reset.setEnabled(False)
        toolbar.addAction(self._act_reset)

        layout.addWidget(toolbar)
```

변경 후:
```python
        toolbar.addSeparator()

        self._act_reset = QAction("원본 복원", self)
        self._act_reset.setToolTip("raw_events 기준으로 events를 초기화합니다")
        self._act_reset.triggered.connect(self._reset_to_raw)
        self._act_reset.setEnabled(False)
        toolbar.addAction(self._act_reset)

        toolbar.addSeparator()

        self._chk_relative_time = QCheckBox("⏱ 상대 시간")
        self._chk_relative_time.setToolTip(
            "체크: 시간(ms) 열을 이전 이벤트 대비 delta로 표시\n"
            "해제: 녹화 시작 기준 절대 시간 표시"
        )
        self._chk_relative_time.setChecked(False)
        self._chk_relative_time.toggled.connect(self._on_relative_time_toggled)
        toolbar.addWidget(self._chk_relative_time)

        layout.addWidget(toolbar)
```

- [ ] **Step 7: `__init__`에 `_relative_time` 플래그 추가**

`EventEditorWidget.__init__`에 `self._relative_time = False` 추가:

기존 (`self._setup_ui()` 직전 줄들):
```python
        self._last_highlight_row: int = -1
        self._setup_ui()
```

변경 후:
```python
        self._last_highlight_row: int = -1
        self._relative_time: bool = False
        self._setup_ui()
```

- [ ] **Step 8: `_on_relative_time_toggled` 메서드 추가**

`cancel_f6_capture` 메서드 뒤 (또는 `_refresh` 앞) 어느 위치든 적절한 곳에 추가:

```python
    def _on_relative_time_toggled(self, checked: bool) -> None:
        """상대 시간 체크박스 토글 — 테이블을 다시 렌더링한다."""
        self._relative_time = checked
        self._refresh()
```

- [ ] **Step 9: `_refresh`의 시간 열 렌더링 변경**

기존 (line 772):
```python
                _cell(f"{row.time_ms:.0f}"),
```

변경 후:
```python
                _cell(f"{row.time_ms_rel:.0f}" if self._relative_time else f"{row.time_ms:.0f}"),
```

- [ ] **Step 10: ruff check + 전체 pytest + 커밋**

```bash
cd /root/.openclaw/workspace/macroflow
ruff check .
source .venv/bin/activate && python3 -m pytest tests/ -v
```

Expected: all pass.

```bash
git add src/macroflow/ui/editor.py tests/test_editor_relative_time.py
git commit -m "feat(editor): relative time checkbox in toolbar (NEW-02)"
```

---

## Task 3: NEW-03 클릭 이벤트 추가 다이얼로그

**Files:**
- Modify: `src/macroflow/ui/editor.py` (`_context_menu` 메서드, `_insert_click` 신규 메서드 추가)

### 구현

현황: 우클릭 메뉴에 텍스트 입력 추가만 있음.
요구사항: "🖱 클릭 추가(&L)..." 메뉴 항목. 다이얼로그: X/Y 위치, F6 직접 지정, 버튼 종류(좌/우/더블), 딜레이.

- [ ] **Step 1: `_insert_click` 메서드 구현**

`_insert_text_input` 메서드 바로 뒤에 추가:

```python
    def _insert_click(self, row_idx: int) -> None:
        """선택 행 다음에 MouseButtonEvent (클릭/더블클릭)를 삽입한다.

        다이얼로그: 위치(X/Y%), F6 직접 지정, 버튼 종류, 딜레이.
        좌/우클릭: mouse_down + mouse_up 2개 이벤트.
        더블클릭: down+up+down+up 4개 이벤트, 50ms 간격.
        첫 번째 mouse_down에만 delay_override_ms 적용.
        """
        if self._macro is None:
            return

        dialog = QDialog(self)
        dialog.setWindowTitle("클릭 추가")
        dialog.setFixedWidth(360)

        layout_v = QVBoxLayout(dialog)

        # ── 위치 ────────────────────────────────────────────────────────────
        form = QFormLayout()
        x_spin = QDoubleSpinBox()
        x_spin.setRange(-500.0, 500.0)
        x_spin.setDecimals(2)
        x_spin.setSuffix(" %")
        x_spin.setValue(50.0)
        form.addRow("X 위치:", x_spin)

        y_spin = QDoubleSpinBox()
        y_spin.setRange(-500.0, 500.0)
        y_spin.setDecimals(2)
        y_spin.setSuffix(" %")
        y_spin.setValue(50.0)
        form.addRow("Y 위치:", y_spin)
        layout_v.addLayout(form)

        # ── F6 직접 지정 ────────────────────────────────────────────────────
        capture_label = QLabel("")
        capture_label.setAlignment(Qt.AlignmentFlag.AlignCenter)
        capture_label.setStyleSheet("color: #c07000; font-weight: bold;")

        btn_capture = QPushButton("📍 화면에서 직접 지정 (F6으로 지정)")
        btn_capture.setToolTip("버튼 클릭 후 원하는 위치로 마우스를 이동하고 F6을 누르세요.")

        captured_color: list[str] = []  # F6 사용 시 색 저장

        def _on_f6_captured(x_r: float, y_r: float, color: str) -> None:
            captured_color.clear()
            captured_color.append(color)
            x_spin.setValue(x_r * 100)
            y_spin.setValue(y_r * 100)
            capture_label.setText(f"✅ 캡처됨: ({x_r * 100:.1f}%, {y_r * 100:.1f}%)")
            dialog.showNormal()
            dialog.raise_()
            btn_capture.setEnabled(True)

        def _start_capture() -> None:
            btn_capture.setEnabled(False)
            capture_label.setText("⏳ F6을 눌러 위치를 지정하세요...")
            self._f6_capture_cb = _on_f6_captured
            self.f6_capture_started.emit()
            dialog.showMinimized()

        def _on_dialog_finished(_result: int) -> None:
            self.cancel_f6_capture()

        btn_capture.clicked.connect(_start_capture)
        dialog.finished.connect(_on_dialog_finished)
        layout_v.addWidget(btn_capture)
        layout_v.addWidget(capture_label)

        # ── 버튼 종류 ────────────────────────────────────────────────────────
        btn_group = QGroupBox("버튼 종류")
        btn_layout = QHBoxLayout(btn_group)
        radio_left = QRadioButton("좌클릭")
        radio_right = QRadioButton("우클릭")
        radio_double = QRadioButton("더블클릭")
        radio_left.setChecked(True)
        btn_layout.addWidget(radio_left)
        btn_layout.addWidget(radio_right)
        btn_layout.addWidget(radio_double)
        layout_v.addWidget(btn_group)

        # ── 딜레이 ──────────────────────────────────────────────────────────
        delay_form = QFormLayout()
        delay_spin = QSpinBox()
        delay_spin.setRange(0, 30000)
        delay_spin.setValue(1000)
        delay_spin.setSuffix(" ms")
        delay_form.addRow("딜레이:", delay_spin)
        layout_v.addLayout(delay_form)

        # ── 버튼 ────────────────────────────────────────────────────────────
        buttons = QDialogButtonBox(
            QDialogButtonBox.StandardButton.Ok | QDialogButtonBox.StandardButton.Cancel
        )
        buttons.accepted.connect(dialog.accept)
        buttons.rejected.connect(dialog.reject)
        layout_v.addWidget(buttons)

        if dialog.exec() != QDialog.DialogCode.Accepted:
            return

        x_ratio = x_spin.value() / 100.0
        y_ratio = y_spin.value() / 100.0
        delay_ms = delay_spin.value()
        rec_color = captured_color[0] if captured_color else None

        if radio_double.isChecked():
            button_str = "left"
        elif radio_right.isChecked():
            button_str = "right"
        else:
            button_str = "left"

        is_double = radio_double.isChecked()
        budget_ns = max(delay_ms * 1_000_000, 1_000_000)

        rows = self._selected_row_indices()
        if rows:
            last_row = self._rows[rows[-1]]
            insert_after_event_idx = max(last_row.event_indices)
        else:
            insert_after_event_idx = len(self._macro.events) - 1

        evs = self._macro.events
        if 0 <= insert_after_event_idx < len(evs):
            base_ts = evs[insert_after_event_idx].timestamp_ns
        elif evs:
            base_ts = evs[-1].timestamp_ns
        else:
            base_ts = 0

        new_events: list[AnyEvent] = []
        _50ms = 50_000_000
        _100ms = 100_000_000

        def _make_down(ts: int, dly: int | None) -> MouseButtonEvent:
            return MouseButtonEvent(
                id=secrets.token_hex(4), type="mouse_down", timestamp_ns=ts,
                x_ratio=x_ratio, y_ratio=y_ratio, button=button_str,
                delay_override_ms=dly,
                recorded_color=rec_color,
            )

        def _make_up(ts: int) -> MouseButtonEvent:
            return MouseButtonEvent(
                id=secrets.token_hex(4), type="mouse_up", timestamp_ns=ts,
                x_ratio=x_ratio, y_ratio=y_ratio, button=button_str,
            )

        if is_double:
            # down(+budget) up(+budget+50ms) down(+budget+100ms) up(+budget+150ms)
            new_events = [
                _make_down(base_ts + budget_ns, delay_ms if delay_ms > 0 else None),
                _make_up(base_ts + budget_ns + _50ms),
                _make_down(base_ts + budget_ns + _100ms, None),
                _make_up(base_ts + budget_ns + _100ms + _50ms),
            ]
            total_budget = budget_ns + _100ms + _50ms
        else:
            # down(+budget) up(+budget+100ms)
            new_events = [
                _make_down(base_ts + budget_ns, delay_ms if delay_ms > 0 else None),
                _make_up(base_ts + budget_ns + _100ms),
            ]
            total_budget = budget_ns + _100ms

        self._push_undo()
        events = list(self._macro.events)
        for offset, ne in enumerate(new_events):
            events.insert(insert_after_event_idx + 1 + offset, ne)

        # 삽입 지점 이후 기존 이벤트 타임스탬프 시프트
        for i in range(insert_after_event_idx + 1 + len(new_events), len(events)):
            ev = events[i]
            events[i] = dataclasses.replace(ev, timestamp_ns=ev.timestamp_ns + total_budget)

        self._apply_events(events)
```

- [ ] **Step 2: 컨텍스트 메뉴에 "🖱 클릭 추가" 추가**

`_context_menu`에서 `act_text_insert` 블록 바로 뒤에 추가한다:

기존:
```python
            act_text_insert = menu.addAction("💬 텍스트 입력 추가(&T)...")
            assert act_text_insert is not None
            act_text_insert.triggered.connect(lambda: self._insert_text_input(rows[0]))

            act_remark = menu.addAction("📝 비고 편집(&N)...")
```

변경 후:
```python
            act_text_insert = menu.addAction("💬 텍스트 입력 추가(&T)...")
            assert act_text_insert is not None
            act_text_insert.triggered.connect(lambda: self._insert_text_input(rows[0]))

            act_click_insert = menu.addAction("🖱 클릭 추가(&L)...")
            assert act_click_insert is not None
            act_click_insert.triggered.connect(lambda: self._insert_click(rows[0]))

            act_remark = menu.addAction("📝 비고 편집(&N)...")
```

- [ ] **Step 3: ruff check + 전체 pytest + 커밋**

```bash
cd /root/.openclaw/workspace/macroflow
ruff check .
source .venv/bin/activate && python3 -m pytest tests/ -v
```

Expected: all pass.

```bash
git add src/macroflow/ui/editor.py
git commit -m "feat(editor): insert click event dialog with F6 capture (NEW-03)"
```

---

## Task 4: NEW-05 즐겨찾기 검색 필터

**Files:**
- Modify: `src/macroflow/ui/favorites.py` (imports, `_setup_ui`, `_apply_search_filter` 신규)
- Create: `tests/test_favorites_search.py`

### 구현

현황: FavoritesWidget에 검색 UI 없음. `favorites.py`는 mypy strict 검사 대상 — 타입 어노테이션 필수.
요구사항: 트리 상단에 QLineEdit 검색창 추가. 실시간 textChanged → `_apply_search_filter`. 검색어 없으면 전부 표시; 검색어 있으면 이름에 검색어 포함되는 항목만 표시(대소문자 무시), 보이는 자식이 없는 그룹은 숨김.

- [ ] **Step 1: 테스트 작성 (실패 확인용)**

`tests/test_favorites_search.py` 파일 생성:

```python
"""즐겨찾기 검색 필터 단위 테스트."""

from __future__ import annotations

import pytest

# favorites.py는 PyQt6 의존 — CI(Linux)에서 헤드리스 모드 필요
# pytest는 QApplication 없이 실행되므로 import 자체가 실패할 수 있음.
# → 이 테스트는 _apply_search_filter 로직을 직접 테스트하기 어려우므로
#    함수 추출이 아닌 존재 여부만 검증한다.

def test_favorites_widget_has_apply_search_filter() -> None:
    """FavoritesWidget에 _apply_search_filter 메서드가 있어야 한다."""
    from macroflow.ui.favorites import FavoritesWidget
    assert hasattr(FavoritesWidget, "_apply_search_filter")
    assert callable(getattr(FavoritesWidget, "_apply_search_filter"))


def test_favorites_widget_has_search_box_attribute() -> None:
    """FavoritesWidget 클래스에 _search_box 속성이 정의되어야 한다."""
    import inspect
    from macroflow.ui.favorites import FavoritesWidget
    src = inspect.getsource(FavoritesWidget._setup_ui)  # type: ignore[misc]
    assert "_search_box" in src
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

```bash
cd /root/.openclaw/workspace/macroflow
source .venv/bin/activate && python3 -m pytest tests/test_favorites_search.py -v
```

Expected: `AttributeError` 또는 FAILED (아직 구현 전)

- [ ] **Step 3: `favorites.py` imports에 `QLineEdit` 추가**

기존:
```python
from PyQt6.QtWidgets import (
    QAbstractItemView,
    QInputDialog,
    QLabel,
    QMenu,
    QMessageBox,
    QToolBar,
    QTreeWidget,
    QTreeWidgetItem,
    QVBoxLayout,
    QWidget,
)
```

변경 후:
```python
from PyQt6.QtWidgets import (
    QAbstractItemView,
    QInputDialog,
    QLabel,
    QLineEdit,
    QMenu,
    QMessageBox,
    QToolBar,
    QTreeWidget,
    QTreeWidgetItem,
    QVBoxLayout,
    QWidget,
)
```

- [ ] **Step 4: `_setup_ui`에 검색창 추가**

`_setup_ui`에서 `layout.addWidget(self._hint)` 뒤, `# 트리 위젯` 블록 앞에 추가:

기존:
```python
        layout.addWidget(self._hint)

        # 트리 위젯
        self._tree = FavoritesTreeWidget(self)
```

변경 후:
```python
        layout.addWidget(self._hint)

        # 검색창
        self._search_box = QLineEdit()
        self._search_box.setPlaceholderText("🔍 즐겨찾기 검색...")
        self._search_box.setClearButtonEnabled(True)
        self._search_box.textChanged.connect(self._apply_search_filter)
        layout.addWidget(self._search_box)

        # 트리 위젯
        self._tree = FavoritesTreeWidget(self)
```

- [ ] **Step 5: `_apply_search_filter` 메서드 구현**

`FavoritesWidget` 클래스의 `_refresh_tree` 메서드 뒤 (또는 적절한 위치)에 추가:

```python
    def _apply_search_filter(self, text: str) -> None:
        """검색어에 따라 트리 항목 표시/숨김을 적용한다.

        검색어가 비어있으면 모두 표시.
        검색어가 있으면 이름에 검색어(대소문자 무시)가 포함된 항목만 표시.
        보이는 자식이 없는 그룹은 숨김.
        """
        q = text.strip().lower()
        root = self._tree.invisibleRootItem()
        group_count = root.childCount()

        for g_idx in range(group_count):
            group_item = root.child(g_idx)
            if group_item is None:
                continue
            child_count = group_item.childCount()
            any_visible = False

            for c_idx in range(child_count):
                child = group_item.child(c_idx)
                if child is None:
                    continue
                if not q:
                    child.setHidden(False)
                    any_visible = True
                else:
                    match = q in child.text(0).lower()
                    child.setHidden(not match)
                    if match:
                        any_visible = True

            # 그룹 자체: 검색어 없으면 항상 표시, 있으면 자식 표시 여부에 따라
            if not q:
                group_item.setHidden(False)
            else:
                group_item.setHidden(not any_visible)
                if any_visible:
                    group_item.setExpanded(True)
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
source .venv/bin/activate && python3 -m pytest tests/test_favorites_search.py -v
```

Expected: 2 passed.

- [ ] **Step 7: ruff check + mypy (favorites.py는 strict 대상)**

```bash
cd /root/.openclaw/workspace/macroflow
ruff check .
source .venv/bin/activate && mypy src/
```

Expected: no errors.

- [ ] **Step 8: 전체 pytest + 커밋**

```bash
source .venv/bin/activate && python3 -m pytest tests/ -v
```

Expected: all pass.

```bash
git add src/macroflow/ui/favorites.py tests/test_favorites_search.py
git commit -m "feat(favorites): real-time search filter (NEW-05)"
```

---

## 최종 검증

- [ ] **전체 품질 검사**

```bash
cd /root/.openclaw/workspace/macroflow
ruff check .
source .venv/bin/activate && mypy src/
source .venv/bin/activate && python3 -m pytest tests/ -v
```

Expected: ruff 0 errors, mypy 0 errors, all tests pass.

- [ ] **pyproject.toml 버전 업데이트**

`pyproject.toml`의 `version = "1.1.0"`을 `version = "1.2.0"`으로 변경.

- [ ] **최종 커밋 + push**

```bash
git add pyproject.toml
git commit -m "chore: bump version to 1.2.0"
git push origin main
```

---

## 셀프 리뷰 체크리스트

**스펙 커버리지:**
- NEW-01: ✅ Task 1 — delay spinbox + delay_override_ms 저장
- NEW-02: ✅ Task 2 — time_ms_rel 필드 + 체크박스 + 렌더링 분기
- NEW-03: ✅ Task 3 — _insert_click + context menu
- NEW-04: ✅ Task 1 — captured_color closure + recorded_color 업데이트
- NEW-05: ✅ Task 4 — _search_box + _apply_search_filter

**타입 일관성:**
- `captured_color: list[str]` — Task 1(NEW-04)과 Task 3(NEW-03) 동일 패턴
- `button_str: str` — `_make_down`/`_make_up` 클로저에서 사용
- `time_ms_rel: float = 0.0` — `_DisplayRow` 기본값 있으므로 기존 코드 호환

**mypy 주의사항:**
- `favorites.py`: strict 대상. `_apply_search_filter`의 `self._search_box` 참조는 `__init__` 전에 `_setup_ui`가 호출되므로 OK.
- `editor.py`: `ignore_errors = true` 대상. mypy 오류 걱정 없음.
